nextflow_pipeline {

    name "Test gsutil"
    script "../main.nf"
    tag "module"
    process "GSUTIL"

    test("-profile gsutil test") {

        options "-stub"

        when {
            params {
                outdir = "${outputDir}"
            }
            process {
                """
                input[0] = Channel.of([
                    [ id: 'url' ], 
                    path('link')
                ])

                """

            }
        }

        then {
            assertAll(

                { assert process.success },
                { assert snapshot(process.out).match() }
            
            )
        }
    }
}
